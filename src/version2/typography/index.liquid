---
title: Typography
description: These are all the typographic HTML elements.
layout: elements
css: version2/simple.css
tags: version2
---
<ul>
  {% for nav in collections.typography reversed %}
    <li {% if page.url == nav.url %}aria-current="page"{% endif %}>
      <a href="{{ nav.url }}">{{ nav.data.title }}</a>
    </li>
  {% endfor %}
</ul>